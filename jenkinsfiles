pipeline {
    agent {label "all"}
    stages {
         stage ('invocationshells crpt') {
            steps {
		    sh'chmod 777 vm1.sh'
                sh 'vm1.sh' 
            }
        }
    }git 

}

